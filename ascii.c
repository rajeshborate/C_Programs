//Write a C program to generate the equivalent ASCII value for a given character.

#include<stdio.h>
int main()
{
    char c;
    scanf("%c",&c);
    printf("%d",c);
    //fill the code
    return 0;
}
